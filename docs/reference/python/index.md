# Python API reference

The `mqces` Python package re-exports the C++ core through a thin
NumPy-friendly wrapper. The top-level surface is documented below; the
underlying nanobind extension (`mqces._mqces_core`) is treated as an
implementation detail.

```{eval-rst}
.. automodule:: mqces
   :members:
   :show-inheritance:
```
